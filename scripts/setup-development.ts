/**
 * Retired: school identity fixtures belong to SMZ Auth.
 *
 * Start the CMS database normally, then seed Auth with its development-school
 * tooling. CMS receives the live directory through the protected Auth API.
 */
console.error('setup-development is retired: seed people, families, classes, and memberships in SMZ Auth.')
process.exitCode = 1
