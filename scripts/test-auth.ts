/**
 * Retired: local Supabase user-role checks are not valid after Auth ownership.
 */
console.error('test-auth is retired: verify CMS access through the SMZ Auth OIDC and directory contracts.')
process.exitCode = 1
