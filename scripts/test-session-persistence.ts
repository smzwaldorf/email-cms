/**
 * Retired: browser session validation now belongs to the CMS server-session
 * and SMZ Auth integration tests, not a local user_roles fixture.
 */
console.error('test-session-persistence is retired: run the CMS/Auth session integration tests instead.')
process.exitCode = 1
